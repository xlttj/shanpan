node_modules/
dist/
*.js.map

# The graph is derived and disposable; the knowledge records are the source of
# truth and must be committed. Note the '/*' form — git cannot re-include a
# file whose parent directory is itself excluded.
.shanpan/*
!.shanpan/knowledge.ndjson
!.shanpan/knowledge.archive.ndjson

# Generated by `shanpan rules` from knowledge.ndjson — derived like graph.db.
# Hand-written rules (no shanpan- prefix) remain committable.
.cursor/rules/shanpan-*.mdc

# Generated by shanpan init/upgrade from src/opencode/shanpan-drift.plugin.ts
.opencode/plugin/shanpan-drift.ts

# Generated by shanpan init/upgrade from src/skills/index.ts. Every shanpan
# skill shares the "knowledge-" prefix, so this ignores the generated copies
# (which would otherwise fork the source of truth) while leaving hand-written
# skills — any other name — and settings.json / hooks.json committable, so a
# team can share the setup.
.claude/skills/knowledge-*/
.cursor/skills/knowledge-*/
.opencode/skills/knowledge-*/

# .shanpanrc.json is deliberately NOT ignored. It is the one file that declares
# a repository uses shanpan at all — everything else here is either derived or
# generated — and it is where shared settings live, so a teammate's clone
# analyzes the same languages and paths rather than silently falling back to
# the defaults.
